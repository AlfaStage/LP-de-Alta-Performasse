
"use client";

// This file is no longer used and can be deleted.
// The functionality has been moved into TrackingScriptsWrapper.tsx
// and the fpixel.ts library.

// Keeping it empty for now to avoid breaking existing imports immediately,
// but it should be removed in a follow-up.
export default function FacebookPixelScript() {
  return null;
}
